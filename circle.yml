machine:
  services:
    - docker
dependencies:
  cache_directories:
    - "~/docker-ubuntu"
  override:
    - docker info
    - if [[ -e ~/docker-ubuntu/image.tar ]]; then docker load --input ~/docker-ubuntu/image.tar; fi
    - docker build -t stefaniuk/ubuntu .
    - mkdir -p ~/docker-ubuntu; docker save --output ~/docker-ubuntu/image.tar stefaniuk/ubuntu
test:
  override:
    - docker run -it stefaniuk/ubuntu echo "Hello World!"
